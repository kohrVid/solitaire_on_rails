<% if flash.present? %>
	<% flash.each do |key, value| %>
		<div class = "alert alert-<%=key%>">
			<div class = "<%= key %>">
				<%= value %>
				<%# if @object.present? && @object.errors.any? %>
			</div>
		</div>
	<%end%>
<%end%>


